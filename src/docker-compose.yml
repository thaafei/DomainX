secrets:
  github_app_pem:
    file: ./infra/secrets/github_app.pem

services:
  redis:
    image: redis:7-alpine
    env_file:
      - .env
    command: >
      sh -c 'redis-server /usr/local/etc/redis/redis.conf --requirepass "$$REDIS_PASSWORD"'
    volumes:
      - ./infra/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis_data:/data
    restart: always
    networks:
      - domainx-net


  backend:
    build: ./backend
    env_file:
      - .env
    command: gunicorn DomainX.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 120
    depends_on:
      redis:
        condition: service_started
    volumes:
      - staticfiles:/app/staticfiles
      - gitstats_data:/data/gitstats
    restart: always
    networks:
      - domainx-net
    secrets:
      - github_app_pem
    extra_hosts:
      - "host.docker.internal:host-gateway"


  celery:
    build: ./backend
    env_file:
      - .env
    command: celery -A DomainX worker -l info
    depends_on:
      backend:
        condition: service_started
      redis:
        condition: service_started
    restart: always
    networks:
      - domainx-net
    secrets:
      - github_app_pem
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - gitstats_data:/data/gitstats


  celery_beat:
    build: ./backend
    env_file:
      - .env
    command: celery -A DomainX beat -l info
    depends_on:
      redis:
        condition: service_started
    restart: always
    networks:
      - domainx-net
    secrets:
      - github_app_pem
    extra_hosts:
      - "host.docker.internal:host-gateway"

  celery_gitstats:
    build: ./backend
    env_file:
      - .env
    command: celery -A DomainX worker -l info -Q gitstats --concurrency=2
    depends_on:
      backend:
        condition: service_started
      redis:
        condition: service_started
    restart: always
    networks:
      - domainx-net
    secrets:
      - github_app_pem
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - gitstats_data:/data/gitstats



  web:
    build: ./frontend
    depends_on:
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/nginx/conf.d:/etc/nginx/conf.d:ro
      - ./infra/nginx/certs:/etc/letsencrypt:ro
      - ./infra/nginx/www:/var/www/certbot:ro
      - ./infra/nginx/selfsigned:/etc/nginx/selfsigned:ro
      - gitstats_data:/data/gitstats:ro
    restart: always
    networks:
      - domainx-net

volumes:
  redis_data:
  staticfiles:
  gitstats_data:

networks:
  domainx-net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.50.0.0/24
          gateway: 10.50.0.1
