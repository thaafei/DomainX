services:
  db:
    image: mysql:8.0
    env_file:
      - .env
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost -p$$MYSQL_ROOT_PASSWORD --silent"]
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - domainx-net

  redis:
    image: redis:7-alpine
    command: >
      sh -c 'redis-server /usr/local/etc/redis/redis.conf
      --requirepass "$$REDIS_PASSWORD"'
    volumes:
      - ./infra/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
      - redis_data:/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", 'redis-cli -a "$$REDIS_PASSWORD" ping | grep -q PONG']
      interval: 5s
      timeout: 5s
      retries: 20
    networks:
      - domainx-net

  backend:
    build: ./backend
    command: gunicorn DomainX.wsgi:application --bind 0.0.0.0:8000 --workers 3 --timeout 120
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - staticfiles:/app/staticfiles
      - /opt/domainx/secrets/github_app.pem:/run/secrets/github_app.pem:ro
    restart: always
    networks:
      - domainx-net

  celery:
    build: ./backend
    command: celery -A DomainX worker -l info
    depends_on:
      backend:
        condition: service_started
      redis:
        condition: service_healthy
      db:
        condition: service_healthy
    volumes:
      - /opt/domainx/secrets/github_app.pem:/run/secrets/github_app.pem:ro
    restart: always
    networks:
      - domainx-net

  celery_beat:
    build: ./backend
    command: celery -A DomainX beat -l info
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_healthy
    volumes:
      - /opt/domainx/secrets/github_app.pem:/run/secrets/github_app.pem:ro
    restart: always
    networks:
      - domainx-net

volumes:
  mysql_data:
  redis_data:
  staticfiles:

networks:
  domainx-net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.50.0.0/24
          gateway: 10.50.0.1
